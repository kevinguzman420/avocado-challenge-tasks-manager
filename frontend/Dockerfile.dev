# Frontend Development Dockerfile
FROM cypress/browsers:latest

# Install pnpm (most cypress images already have npm, but pnpm needs to be installed if not present)
RUN npm install -g pnpm

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package.json pnpm-lock.yaml ./
RUN pnpm install

# Cypress binary is usually pre-installed in these images, but explicitly installing again ensures it
RUN npx cypress install --force

# Expose Vite port and start dev server
EXPOSE 5173
CMD ["pnpm", "run", "dev", "--host", "0.0.0.0"]
